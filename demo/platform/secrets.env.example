# Copy to secrets.env for local platform demo (never commit secrets.env).
# Gateway accepts these keys on /v1/chat/completions when GATEWAY_API_KEYS is set.

GATEWAY_API_KEYS=demo-local-key
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
