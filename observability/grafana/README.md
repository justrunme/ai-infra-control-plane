# Grafana

This directory will hold dashboards for:

- model availability
- request latency
- backend capacity
- estimated hourly cost
- Kubernetes resource usage

The first dashboard should be added after Prometheus metrics are exposed by the control API.

